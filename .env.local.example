# =============================================================================
# Database connection (Supabase = PostgreSQL + Auth)
# =============================================================================
# Copy this file to .env.local and fill in your values.
# Get URL and anon key: Supabase Dashboard → Project Settings → API
# =============================================================================

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key


# Optional: enable /test-smtp page to test custom SMTP (sends a recovery email)
# NEXT_PUBLIC_ALLOW_TEST_SMTP=true

# Optional: server-only (bypasses RLS; use only in API routes / server code)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# Phase 6: FBR middleware (leave commented until Phase 6)
# =============================================================================
# FBR_MIDDLEWARE_URL=https://your-middleware.vercel.app
# FBR_MIDDLEWARE_API_KEY=your-secret-key
